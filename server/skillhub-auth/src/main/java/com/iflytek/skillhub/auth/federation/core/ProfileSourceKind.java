package com.iflytek.skillhub.auth.federation.core;

/** Platform-defined profile source families; protocol and vendor names do not belong here. */
public enum ProfileSourceKind {
    LOGIN_ASSERTION,
    USER_MANAGED,
    ORGANIZATION_ADMIN
}
