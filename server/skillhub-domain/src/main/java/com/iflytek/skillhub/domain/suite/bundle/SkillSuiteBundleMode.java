package com.iflytek.skillhub.domain.suite.bundle;

/** Whether a Bundle creates a Suite or derives a new draft from an existing Suite version. */
public enum SkillSuiteBundleMode {
    CREATE,
    UPDATE
}
