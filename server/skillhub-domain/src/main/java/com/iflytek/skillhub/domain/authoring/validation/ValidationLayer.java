package com.iflytek.skillhub.domain.authoring.validation;

/**
 * The three validation layers executed by every run, in order.
 */
public enum ValidationLayer {
    STRUCTURE,
    CONFIG,
    BEHAVIOR
}
