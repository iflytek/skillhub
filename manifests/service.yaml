apiVersion: v1
kind: Service
metadata:
  name: skillhub-scanner
  labels:
    app: skillhub-scanner
spec:
  type: ClusterIP
  selector:
    app: skillhub-scanner
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  sessionAffinity: None
