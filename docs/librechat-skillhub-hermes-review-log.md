# LibreChat SkillHub Hermes Documentation Review Log

## Status

Patched after fifth review. Review dispositions are recorded in-repo, the PRD is explicitly unignored for normal git review, and the Phase 0 fixture pack exists under `docs/contracts/librechat-skillhub-hermes/v1/`. The fixture pack is a seed contract, but Phase 0 is not complete until each participating repo has a real fixture validation command. SkillHub lifecycle code alignment for pending-review withdrawal and direct-publish scanner distribution gating also remain unresolved until the implementation changes land.

## Review Scope

The documentation set under review:

- `docs/prds/librechat-skillhub-hermes-prd.md`
- `docs/librechat-skillhub-hermes-design.md`
- `docs/librechat-skillhub-hermes-implementation-plan.md`
- `docs/librechat-skillhub-hermes-review-log.md`
- `docs/contracts/librechat-skillhub-hermes/v1/`

The first pass reviewed the PRD, design, and implementation plan with two independent reviewers per document. The second pass repeated that coverage and added two reviewers for this review log. The third pass repeated the same review pattern after the second patch. The fourth and fifth passes reviewed the PRD, design, implementation plan, review log, and fixture pack after the subsequent patches.

## Review Provenance

| Pass | Target | Reviewer Threads | Notes |
| --- | --- | --- | --- |
| First pass | PRD, design, implementation plan | Six independent review agents. | Raw notes are not repo-addressable; only the summarized dispositions in this log should be used for durable traceability. |
| Second pass | PRD | `019e775e-fba2-7683-a115-df99356dfbac`, `019e775e-fc05-7810-9aa8-10e907ef2e63` | Focused on product completeness, lifecycle authority, acceptance coverage, and standalone-mode safety. |
| Second pass | Design | `019e775e-fc6e-78b0-936c-1c66aef61648`, `019e775e-fcd4-73c3-bd0b-25d1012882c0` | Focused on resource modeling, token semantics, endpoint contract readiness, and lifecycle consistency. |
| Second pass | Implementation plan | `019e775e-fd58-72b3-b649-c50f9c781bd3`, `019e775e-fddb-7a01-9da6-4db79523135e` | Focused on fixture handoff, rollout safety, token tests, rollback tests, and verification commands. |
| Second pass | Review log | `019e7760-4c8d-7471-8303-f230dad74e99`, `019e7760-4cf0-7de0-8494-393b7cbe1de2` | Focused on disposition accuracy, traceability, and audit usefulness. |
| Third pass | PRD | `019e7767-98d6-7a20-bed8-5caebef6fb7d`, `019e7767-9942-7bc3-8dc9-b74e0b3b4a9f` | Focused on RBAC granularity, fingerprint contract, local-skill scope, security acceptance, and lifecycle acceptance. |
| Third pass | Design | `019e7767-99b9-7cb2-abd5-c3f435b66b6e`, `019e7767-9a31-7df2-89b6-be3fa6b477e0` | Focused on runtime download authorization, review-task inventory, token transport, version ID safety, hashing, and rollout constraints. |
| Third pass | Implementation plan | `019e7767-9ad6-7f53-b5a8-214fa6d0c27e`, `019e7767-9b7f-75c1-9a21-b743ab196777` | Focused on fixture gate hardness, report-only no-token safety, service auth tests, flag matrices, audit-read scope, and executable rollout commands. |
| Third pass | Review log | `019e7769-3bef-7ae2-bb44-7f94d911f1c9`, `019e7769-3c4f-7a61-b149-f609603bfbc8` | Focused on unresolved disposition claims, durable provenance, PRD git visibility, and evidence traceability. |
| Fourth pass | PRD | `019e7770-3a52-7c60-af85-37bf32fb77c3`, `019e7770-3ab9-78c0-b97a-fcb9ecd41138` | Focused on lifecycle authority, draft validation authorization, fixture readiness, token security, and acceptance/test handoff. |
| Fourth pass | Design | `019e7770-3b29-75a0-bdaa-21e7a448a627`, `019e7770-3b99-7ea1-b8e3-e25d82415d4d` | Focused on signed-token safety, exact-byte hashing, endpoint responses, review-task scope, and runtime download binding. |
| Fourth pass | Implementation plan | `019e7770-3c0e-7c72-a829-eaa5c85c2f9c`, `019e7770-3c81-7102-b6f0-4e0b9d29b766` | Focused on executable Phase 0 gates, command availability, fixture completeness, and lifecycle-code sequencing. |
| Fourth pass | Review log | `019e7772-9621-7830-8c46-93160b5227f3`, `019e7772-9682-7e52-af4f-aa689c6aa346` | Focused on overstated readiness, unresolved lifecycle/code divergence, durable provenance, and disposition traceability. |
| Fourth pass | Fixture pack | `019e7772-96ee-7832-88ff-2720ce376c0b`, `019e7772-9769-7fe1-8b80-1c54c39d412f` | Focused on exact-byte hash determinism, token rejection coverage, positive responses, error coverage, profile pins, and lifecycle deltas. |
| Fifth pass | PRD | `019e777d-a7e4-7282-ab59-0b84e9e31286`, `019e777d-a852-7370-802b-7f4f12a95d42` | Focused on direct-publish scanner gating, lifecycle migration clarity, report-only consistency, and RBAC action granularity. |
| Fifth pass | Design | `019e777d-a8ad-7813-baf1-27f43f154994`, `019e777d-a95a-7370-81eb-3ae32449cf24` | Focused on one-time download handles, idempotency/token replay ordering, actor identity keys, service principals, and Hermes loader bypass risks. |
| Fifth pass | Implementation plan | `019e777d-a9e4-7ef0-9f4b-083512f9f91c`, `019e777d-aabc-7ce3-b61c-9845c502003b` | Focused on phase boundary alignment, lifecycle prerequisite ownership, revocation-after-sync E2E coverage, and rollout gate commands. |
| Fifth pass | Review log | `019e777f-4a98-7c20-8742-68158ac05d63`, `019e777f-4af7-7330-ad5c-0cfd6cc580cf` | Focused on overstated Phase 0 readiness, unresolved blocker closeout criteria, and durable fifth-pass provenance. |
| Fifth pass | Fixture pack | `019e777f-4b4a-7060-a353-2aeb6d299c40`, `019e777f-4bd3-72d2-8653-3f8c28038452` | Focused on executable negative coverage, exact wire hydrate requests, error/status examples, and download handle semantics. |

## Consolidated Findings And Disposition

The table uses stable document sections and fixture paths while the docs are still changing. Before final implementation handoff, unresolved rows should be converted to issue or PR links with exact verification artifacts.

| Finding | Disposition | Evidence Or Follow-Up |
| --- | --- | --- |
| Direct raw SkillHub APIs could bypass Control Tower in integrated deployment. | Added integrated deployment boundary requirements, direct-bypass acceptance criteria, SkillHub integrated mode, rollout flags, and direct raw route tests. | PRD `Registry Discovery`, `FR-6`, `Acceptance Criteria`; design `Integrated Deployment Boundary`; plan `Phase 2`, `Phase 6`. |
| Signed Control Tower decision tokens were underdefined. | Added required claims, serialization, canonicalization, header name, TTL, clock skew, replay behavior, key rotation, `ALLOW`-only SkillHub tokens, rejection cases, and token binding to audience, method, path, query/body hash, resource, action, workspace, and nonce. | Design `Signed Decision Token Contract`; plan `Phase 1`, `Phase 2`. |
| `LIMIT` decision tokens could bypass unsigned obligations. | SkillHub decision tokens are now issued and accepted only for full `ALLOW`; `LIMIT` may be returned to LibreChat but has no SkillHub token in V1. | Design `Batch Permission Evaluation`, `Service Contract: Control Tower Decisions`, `Signed Decision Token Contract`; plan `Phase 1`, `Phase 2`. |
| Multi-resource hydrate could validate one token and return extra IDs. | Hydrate now requires one `{ resourceId, decisionToken }` per item and SkillHub must validate each token against that item before returning details. | Design `Internal Endpoint Contract`; plan `Phase 2`. |
| Control Tower RBAC vs SkillHub local RBAC was ambiguous. | Clarified that Control Tower authorizes internal integration endpoints while SkillHub lifecycle, package, scanner, ownership, idempotency, and audit invariants remain mandatory. Added explicit permission adapter requirement and banned fabricated local role injection. | PRD `Product Decisions`, `Security And Compliance Requirements`; design `Trusted Decision Validation`, `Lifecycle Enforcement`; plan `Phase 2`. |
| Registry search flow checked policy before candidate resource IDs existed. | Changed design to Control Tower inventory-backed search prefilter followed by SkillHub hydrate for allowed IDs. | Design `Registry Search Flow`; plan `Phase 1`. |
| Hermes sync path was ambiguous and could bypass signed SkillHub authorization. | Chose V1 runtime sync through LibreChat only; Hermes does not call SkillHub directly in integrated mode. | Design `Registry Search Flow`, `Hermes Design`; plan `Phase 5`. |
| Installed pins and Hermes caches were not revalidated after lifecycle or policy changes. | Added server-side pin resolution, lifecycle/policy/scanner revalidation, disabled pin behavior, fingerprint verification, fail-closed runtime behavior, and expanded disabled-pin acceptance coverage. | PRD `Pin Revalidation`, `Failure Matrix`, `Acceptance Criteria`; design `LibreChat Design`, `Hermes Design`, `Failure Modes`; plan `Phase 3`, `Phase 5`. |
| Resource model conflated skill containers and versions. | Introduced `SKILL_VERSION` alongside `SKILL_NAMESPACE` and `SKILL`; added canonical resource ID grammar and parent metadata. | PRD `Registry Discovery`; design `Resource Model`; plan `Shared Contract Artifacts`, `Phase 1`. |
| Review and audit resources were referenced but not modeled. | Added `SKILL_REVIEW_TASK`; audit read checks reuse the resource that scopes the audit request in V1. | Design `Resource Model`, `Actions`; plan `Phase 1`. |
| Resource ID examples contradicted the grammar and path usage was unsafe. | Removed the invalid namespace example and documented that raw SkillHub resource IDs must travel in JSON/query fields with URL encoding, not path segments. Changed governance endpoint to `/governance/actions`. | Design `Resource ID Grammar`, `Internal Endpoints`. |
| `skill.review.decide` incorrectly included submitter withdrawal. | Limited `skill.review.decide` to approve/reject and added `skill.review.withdraw` for submitter-owned withdrawal. | PRD `Admin And Review`, `Action And Resource Matrix`; design `Actions`; plan `Phase 2`. |
| Review withdrawal lifecycle conflicted with the intended `DRAFT` lifecycle target. | Product/design now require submitter-only withdrawal to `DRAFT`; current SkillHub code/tests may still use `UPLOADED`, so this remains an implementation blocker until the Phase 1A code alignment lands. | PRD `Admin And Review`, `Action And Resource Matrix`; design `Draft Publish Flow`; plan `Phase 1A`, `Implementation Slice Matrix`. |
| Direct publish semantics were unclear against SkillHub `SUPER_ADMIN`. | Clarified `skill.publish.direct` is a trusted integration capability, not a blanket `SUPER_ADMIN` mapping, and SkillHub still enforces direct-publish lifecycle constraints. | PRD `Draft And Publish`; design `Lifecycle Enforcement`; plan `Phase 2`. |
| Acceptance criteria were too coarse. | Added requirement IDs, acceptance signals, failure matrix, action/resource matrix, standalone-mode acceptance, phase done signals, and implementation slice matrix. | PRD `Functional Requirements`, `Failure Matrix`, `Acceptance Criteria`; plan `Implementation Slice Matrix`. |
| Shared contract fixtures were not actionable. | Named the canonical fixture path, required fixture files, versioning rule, review owners, and fixture validation expectations. | Plan `Shared Contract Artifacts`, `Phase 0`. |
| Rollout and rollback were underspecified. | Added feature flags, runtime flag dependencies, partial-rollout invariant, rollback state matrix, persisted-state behavior, rollback tests, and operator config ownership. | PRD `Rollout Requirements`; design `Rollout And Compatibility`; plan `Prerequisites And Integration Config`, `Rollout Flags`, `Rollback State Matrix`, `Phase 6`. |
| Runtime flags could be enabled in unsafe combinations. | Required Hermes invoke enforcement and local-present denial tests before Hermes sync can promote bundles for governed runs and before LibreChat sends `requested.skills`. Added flag-matrix test requirement. | PRD `Rollout Requirements`; design `Rollout And Compatibility`; plan `Phase 3`, `Phase 5`, `Rollout Flags`. |
| Token security prerequisites were not fully backed by tests. | Added tests for unknown `kid`, key rotation overlap, clock-skew boundaries, canonical method/path/query/body hashes, replay-store unavailable fail-closed behavior, `LIMIT`/`DENY`, and mismatch cases. | Plan `Phase 1`, `Phase 2`. |
| Verification commands were not actionable enough. | Added SkillHub staging, corrected Hermes pytest argument separator, and defined contract, integrated allowed, integrated denied, and rollback gate command shapes. | Plan `Suggested Verification Commands`. |
| Fixture pack was required but absent. | Added and expanded V1 fixture pack under `docs/contracts/librechat-skillhub-hermes/v1/`; Phase 0 now requires real per-repo fixture validation commands before Phase 1 code merges. | Plan `Shared Contract Artifacts`, `Phase 0`; fixtures under `docs/contracts/librechat-skillhub-hermes/v1/`. |
| Artifact fingerprint was mandatory but undefined. | Defined canonical fingerprint as `sha256:{64 lowercase hex}` over exact stored SkillHub bundle bytes and added fingerprint fixtures. | PRD `Installation And Profile Pins`; design `Artifact Fingerprint Contract`; plan `Shared Contract Artifacts`; fixture `artifact-fingerprints.json`. |
| Runtime download authorization was ambiguous. | Split runtime sync authorization by layer: SkillHub validates `skill.install` for download; LibreChat verifies envelope `skill.invoke` before runtime download; Hermes still performs per-invocation authorization. | Design `Registry Search Flow`, `Internal Endpoints`, `Internal Endpoint Contract`; fixture `internal-api.requests-responses.json`. |
| Hydrate token transport still contradicted the single-header rule. | Carved hydrate out as the only V1 multi-token exception: no decision-token header, one item-level token per resource. | Design `Signed Decision Token Contract`, `Internal Endpoint Contract`; fixture `internal-api.requests-responses.json`. |
| Review-task authorization was modeled but not exported/tested. | Added pending review tasks to capability inventory, lifecycle deltas, fixtures, and plan tests. | Design `Capability Inventory`; plan `Phase 1`, `Implementation Slice Matrix`; fixture `capability-inventory.records.json`. |
| `skill.manage` collapsed actions with different RBAC boundaries. | Split lifecycle/governance mutations into `skill.archive`, `skill.visibility.manage`, `skill.yank`, `skill.label.attach`, and `skill.label.definition.manage`; kept `skill.audit.read` as a separate read action. | PRD `Admin And Review`, `Action And Resource Matrix`; design `Actions`, `Internal Endpoints`; plan `Phase 2`. |
| Report-only rollout lacked no-token/no-mutation safety tests. | Added `CONTROL_TOWER_SKILLHUB_REPORT_ONLY`, no-token would-deny behavior, report-only smoke scenario, and mutation-denial tests. | PRD `Security And Compliance Requirements`; design `Rollout And Compatibility`; plan `Phase 6`, `Rollout Flags`, `Suggested Verification Commands`. |
| Version IDs were unsafe for current unconstrained SkillHub version inputs. | Constrained integrated SkillHub versions to a URL-safe grammar and added implementation-plan validation work plus fixtures for invalid versions. | Design `Resource ID Grammar`; plan `Phase 1`; fixture `resource-ids.invalid.json`. |
| Current code may still withdraw to `UPLOADED`. | Confirmed unresolved implementation blocker. The docs and tests-to-add now require manual and auto-withdraw to `DRAFT`; this row must not be treated as complete until the SkillHub code and tests are patched. | Plan `Phase 1A`, `Implementation Slice Matrix`. |
| PRD was ignored by `.gitignore`. | Added a `.gitignore` exception for this PRD so normal `git status` and `git diff` include it. | `.gitignore`; plan `Phase 0`. |
| Review provenance was thread-only. | Added third-pass and fourth-pass reviewer IDs and durable finding dispositions in this log; future raw transcripts should be archived in repo or linked from issue/PR artifacts when available. | Review log `Review Provenance`, `Consolidated Findings And Disposition`. |
| Draft validation authorization was contradictory. | Made `/drafts/validate` service-auth-only in the design endpoint table and contract text; `/drafts/publish` remains protected by `skill.publish` or `skill.publish.direct`. | PRD `Security And Compliance Requirements`; design `Internal Endpoints`, `Internal Endpoint Contract`; fixture `internal-api.requests-responses.json`. |
| Symmetric HMAC decision tokens would let a verifier mint tokens if compromised. | Changed production token contract to asymmetric Ed25519 signing with Control Tower private keys and SkillHub public verification keyrings. Fixture private keys are explicitly local-test-only. | PRD `Security And Compliance Requirements`; design `Signed Decision Token Contract`; plan `Prerequisites And Integration Config`; fixture `decision-token.allow.json`. |
| Signed body-hash fixtures were not deterministic across languages. | Added exact `bodyUtf8` strings and hashes for token-bound requests; fixture README now requires hashing bytes directly rather than reserializing parsed JSON. | Design `Signed Decision Token Contract`; fixtures `decision-token.allow.json`, `internal-api.requests-responses.json`, `README.md`. |
| Internal API fixtures lacked positive response examples. | Expanded `internal-api.requests-responses.json` with request and response envelopes for capabilities, hydrate, resolve, install/runtime download, draft validate, publish, review decide, withdraw, audit query, and governance yank. | Design `Internal Endpoint Contract`; fixture `internal-api.requests-responses.json`. |
| Error fixtures missed required fail-closed cases. | Expanded error fixtures for invalid resource IDs, contract/version and idempotency errors, denied/expired/replayed tokens, hidden lifecycle state, lifecycle/idempotency conflicts, warning confirmation, scanner blocked, replay-store unavailable, and scanner unavailable. | Design `Internal Endpoint Contract`; fixture `internal-api.errors.json`. |
| Decision-token rejection fixtures were too thin and ambiguous. | Added mutation instructions and rejection cases for wrong path, query hash, action, resource, workspace, expiry, invalid signature, `DENY`, `LIMIT`, stale policy, key rotation, replay, and replay-store outage. | Design `Signed Decision Token Contract`; plan `Phase 1`, `Phase 2`; fixture `decision-token.rejections.json`. |
| Capability and profile-pin fixtures were thinner than documented schemas. | Added namespace/slug/visibility/owner/source metadata, corrected SkillHub enum values, added pending review version coverage, and expanded profile pins with description, installed-by, updated-at, sync, revalidation, and disabled-state fields. | Design `Capability Inventory`, `User Profile Pins`; fixtures `capability-inventory.records.json`, `profile-pin.records.json`. |
| Lifecycle/inventory fixture deltas were too sparse. | Added deltas for pending review creation, approve, reject, withdraw-to-`DRAFT`, stale review-task disablement, yank, hide, restore, archive, scanner-blocked, and delete. | Plan `Phase 1`; fixture `capability-inventory.records.json`. |
| Fixture validation gate was documented but no command exists in SkillHub. | Plan now makes `scripts/validate-skillhub-contract-fixtures.sh` an explicit Phase 0 deliverable and says the command is not yet present; the review log status no longer claims the fixture pack is an exit gate by itself. | Plan `Phase 0`, `Suggested Verification Commands`; review log `Status`. |
| Direct publish could expose artifacts before scanner policy is acceptable. | Added a product/design requirement and Phase 1A prerequisite that direct-published versions are not installable, downloadable, syncable, or invokable until scanner/package policy marks the exact artifact download-ready. | PRD `Product Decisions`, `Draft And Publish`, `Acceptance Criteria`; design `Lifecycle Enforcement`; plan `Phase 1A`. |
| Pre-signed or stream download responses conflicted with single-use authorization. | Chose V1 JSON-envelope download responses with one-time internal bundle handles, short TTL, mTLS/internal audience binding, fingerprint binding, and no raw stream or pre-signed URL from `/skills/download`. | Design `Internal Endpoint Contract`; fixture `internal-api.requests-responses.json`. |
| Idempotency retries conflicted with single-use decision-token replay. | Defined validation order: service auth and idempotency lookup happen before fresh-token consumption; exact same key/body can return stored response; new mutations require fresh tokens; replaying consumed tokens under new keys is rejected. | Design `Internal Endpoint Contract`; fixture `internal-api.errors.json`; plan `Phase 2`. |
| Phase 1 depended on a SkillHub export endpoint that was planned for Phase 2. | Clarified Phase 1 consumes only Phase 0 fixtures; live SkillHub capability export lands in Phase 2 and is accepted end-to-end in Phase 6. | Plan `Shared Contract Artifacts`, `Phase 1`, `Implementation Slice Matrix`. |
| Hermes synced bundles could be discovered by legacy loaders. | Added a loader isolation requirement: synced bundles live outside legacy/local roots and require provenance `source=skillhub`, exact resource ID, envelope membership, and per-invocation authorization before execution. | Design `Hermes Design`; plan `Phase 5`. |
| Fixture negative coverage was not executable enough. | Added missing-token error codes, per-rejection status/envelope expectations, exact wire hydrate token body, parser/claim rejection cases, and negative request stimuli. | Fixtures `decision-token.rejections.json`, `internal-api.errors.json`, `internal-api.requests-responses.json`. |
| Revocation after Hermes sync was not covered end to end. | Added a Phase 6 smoke scenario covering install, sync, successful invoke, lifecycle/policy/scanner revocation, inventory refresh, disabled pin, absent `requested.skills`, and Hermes denial while the bundle remains on disk. | Plan `Phase 6`, `Suggested Verification Commands`. |

## Open Blockers Before Implementation Handoff

| Blocker | Owner | Closeout Evidence |
| --- | --- | --- |
| Fixture validation commands do not exist yet. | SkillHub, Control Tower, LibreChat, Hermes repo owners. | Real commands named in the implementation plan run in CI or local gates and fail on stale hashes, invalid signatures, missing errors, unresolved token refs, and malformed JSON. |
| Pending-review withdrawal still needs code alignment to `DRAFT`. | SkillHub backend owner. | Manual and auto-withdraw tests pass with `PENDING_REVIEW -> DRAFT`; old `UPLOADED` expectations are removed or explicitly migrated. |
| Direct-publish scanner distribution gate is not implemented. | SkillHub backend/security owner. | Direct-published versions cannot resolve/download/sync/invoke through integrated paths until scanner/package policy is acceptable; tests cover scanner pending, failed, unavailable, expired, and blocked. |

## Remaining Future Questions

- Whether V2 should migrate existing LibreChat local skills into SkillHub.
- Whether workspace-owned or agent-owned skill pins should be added after per-user pins are stable.
- Whether administrators need optional direct SkillHub SSO in integrated deployments.
