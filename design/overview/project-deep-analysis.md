# 项目深度分析

## 概览

SkillHub 当前是一个“技能注册中心 + 治理平台”项目，整体由 Java/Spring Boot 多模块后端、React/Vite 前端、以及一套文档站与设计/需求文档组成。

系统已经明显从“阶段性快速演进”进入“功能较完整但文档和历史资产需要收敛”的状态：

- 核心业务能力已经较全：
  - 技能发布、浏览、下载
  - 命名空间与治理
  - 审核、提升、举报
  - 收藏、评分、通知
  - 本地账号、OAuth、Device Auth、密码重置
  - 安全扫描与前端审核展示
- 运行时支持两条主线：
  - 标准 `PostgreSQL + Redis` 模式
  - 轻量 `local-h2` 本地模式
- 仓库历史文档较多，包含大量阶段性计划、专题设计、一次性分析和已实现 PRD，适合进一步收敛。

## 后端架构

后端是 Maven 多模块工程，主模块边界如下：

- `skillhub-app`
  - Spring Boot 启动与控制器入口
  - 组装应用层配置、Flyway、Actuator、Web、限流、扫描接入
- `skillhub-domain`
  - 领域模型、领域服务、业务规则
  - 技能、命名空间、审核、提升、安全、标签等核心对象
- `skillhub-auth`
  - 认证授权、OAuth、本地账号、Device Auth、密码重置、会话能力
- `skillhub-infra`
  - JPA 仓储实现、WebClient/外部集成、持久化适配
- `skillhub-search`
  - 搜索实现
  - 当前同时支持 `postgres` 和 `h2` 两套引擎
- `skillhub-storage`
  - 存储 SPI 与本地/S3 适配
- `skillhub-notification`
  - 通知领域与基础能力

这个划分整体是合理的，优点是：

- 应用入口、领域规则、基础设施、搜索、存储被拆开
- 便于针对不同运行模式替换底层实现
- 本地轻量模式可在不推翻主链路的前提下逐步引入

当前的主要结构问题不在“模块是否存在”，而在“历史演进造成的边界轻微重叠”：

- `app/domain/infra` 的职责局部仍有交叉
- 一些功能是按增量演进叠上去的，导致配置和适配逻辑分散
- 运行时模式从 PostgreSQL-only 演化为 PostgreSQL + H2 后，数据库能力差异开始显性化

## 前端架构

前端基于 React + Vite + TanStack Router/Query，结构以功能域组织为主：

- `web/src/features/*`
  - 按领域拆分功能模块，如 `auth`, `search`, `review`, `skill`, `security-audit`, `namespace`
- `web/src/pages/*`
  - 页面层，包括普通页、dashboard、admin、settings
- `web/src/shared/*`
  - UI 组件、hooks、工具函数
- `web/src/api/*`
  - API client 与生成类型

前端的成熟度已经不低，能看出以下特点：

- 页面和 feature 的分工较明确
- 审核、安全、文件预览、详情页等复杂交互已经具备独立 feature
- 类型生成链路已经进入仓库

主要问题在于：

- 历史专题功能增加较多后，`shared` 与 `features` 的边界仍需持续收口
- 文档里记录过的部分结构问题在代码里已经改善，但文档层没有同步收敛

## 运行模式与基础设施

当前项目实际支持两种开发运行模式：

### 1. 标准模式

- 数据库：PostgreSQL
- 缓存/会话：Redis
- 搜索：PostgreSQL 搜索实现
- Flyway：开启
- 目标：尽量贴近真实生产行为

### 2. `local-h2` 轻量模式

- 数据库：文件型 H2
- Flyway：关闭
- Schema：Hibernate 自动建表
- 搜索：`H2LikeSearchQueryService`
- Redis/Session/Scanner/Device Auth：做不同程度降级或关闭
- 目标：快速本地联调与页面验证

这说明项目已经形成了“生产真实链路”和“开发轻量链路”并存的架构趋势。

优点：

- 减少本地环境依赖
- 便于前后端联调
- 为未来数据库可插拔方向提供试验田

风险：

- H2 与 PostgreSQL 行为不一致会持续制造“本地可用、线上不一定一致”的落差
- 数据库迁移策略、文档与脚本如果继续叠加，会加重维护负担

## 数据、搜索、认证、治理

### 数据层

当前主数据模型已经覆盖：

- 用户/身份绑定/角色权限
- 命名空间/成员关系
- 技能/版本/文件/标签
- 审核/提升/举报
- 收藏/评分
- 通知/通知偏好
- 安全审计

这套模型已经偏平台化，不再是单一 CRUD 应用。

### 搜索

搜索是当前系统里最值得单独维护边界的能力：

- `postgres` 模式承担主搜索能力
- `h2` 模式承担降级兼容能力

这意味着搜索不再只是一个查询接口，而是“有引擎差异的子系统”。  
后续如果继续发展数据库可插拔，搜索层会是最先继续分化的部分。

### 认证

认证体系复杂度已经明显提高：

- OAuth2
- 本地账号
- 会话 bootstrap
- Device Auth
- 密码重置
- 多账号合并

这部分能力已经足以支撑多入口平台，但也意味着：

- 配置项较多
- 文档必须保持清晰，否则接入成本会上升

### 治理

治理链路已经非常完整：

- namespace 生命周期治理
- skill 审核、提升、隐藏/撤回
- 安全扫描与审核展示
- 审计与通知

这使项目明显具备“平台治理后台”属性，而不是单纯的技能市场。

## 主要风险

### 1. 运行模式文档与实际代码分叉

`local-h2`、Flyway 收敛、Redis 剪裁、搜索双实现这些能力已经进入代码，但文档仍有历史叠层，容易误导维护者。

### 2. 文档入口过多

`docs/`、`docs/prds/`、`design/`、`design/implemented/` 多层并存后，如果没有明确分工，新增文档还会继续扩散。

### 3. 历史计划与当前实现混杂

过去的阶段计划、注释分析、专题设计在历史上有价值，但继续留在主路径会降低当前文档信噪比。

### 4. 数据库收敛风险

数据库迁移已经开始做“合并初始化”，这对新库有好处，但对旧库升级、历史数据清理、运维脚本必须有清晰边界，否则容易误用。

## 建议沉淀的设计文档

当前最值得长期保留在 `design/` 下的文档类型是：

1. 项目总览与模块边界
2. 运行模式与环境矩阵
3. 文档治理规则
4. 数据库初始化/重置策略
5. 已实现需求归档

这几类文档比阶段计划更有长期价值，也更适合作为未来维护入口。
