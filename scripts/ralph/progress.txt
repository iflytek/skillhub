## Codebase Patterns
- 对需要“本地 Session 为真源、上游状态检查/登出为可选补充”的企业登录接入，优先把 provider-specific login context 序列化进 servlet session，再由专门 flow service 统一做 status/logout；不要把 access token 或远端调用逻辑塞进 `PlatformPrincipal`。
- 当浏览器以独立前端 origin 访问 SkillHub，而 runtime-config 又把 `apiBaseUrl` 指向绝对后端地址时，除了 `fetch` 要带 `credentials: 'include'` 之外，所有后端下发的相对认证跳转 URL 也必须先经前端 `buildApiUrl`/等价 helper 归一化到后端 origin；否则真实浏览器会把 `/api/v1/auth/...` 导向静态前端站点自身。
- 跨源认证链路里的非跳转 POST（例如 logout）也必须走 `buildApiUrl`/runtime `apiBaseUrl`，不能继续保留裸 `/api/...` 相对路径；否则浏览器会绕过后端真实 endpoint，导致本地 Session 和上游登录态无法同步清理。
- 认证相关跨源验证在这个代码库里应由 Spring Security 的集中式 CORS 配置兜底，并从 `skillhub.public.base-url` 推导允许 origin；不要把 CORS 规则散落到各个 controller 或单独的业务 endpoint 上。
- New auth-provider integrations should normalize upstream provider payloads into small internal DTO/record types inside `skillhub-auth`, so application services never depend on provider-specific contracts.
- When running Maven from `server/pom.xml` with `-pl ... -am -Dtest=...`, add `-Dsurefire.failIfNoSpecifiedTests=false` so upstream modules without matching tests do not fail the build.
- Optional Redis-backed auth helpers in this codebase should choose Redis vs local fallback in a dedicated `@Configuration` bean via `ObjectProvider<RedisTemplate<...>>`, not by scattering cache-mode branches through services.
- Configuration-only behavior here is usually validated with lightweight `Binder` or `ApplicationContextRunner` tests instead of full `@SpringBootTest` contexts.
- Browser login flows here should reuse `OAuthLoginRedirectSupport` for `returnTo` sanitization and default target handling, instead of duplicating redirect validation rules in each auth provider.
- New enterprise auth providers should map a stable upstream identifier into `IdentityBinding.subject` and delegate local user creation through `IdentityBindingService`, rather than introducing fallback matching by display name or email.
- Spring beans in this codebase that keep an extra test-only constructor should mark the production constructor explicitly (for example with `@Autowired`), otherwise enabling the bean can fail with `No default constructor found`.
- Conditional auth endpoints that depend on provider beans need a local/mock provider implementation for runtime/OpenAPI verification; otherwise code can compile and tests can pass while the real app still exposes 404s for the new routes.
- Browser callback flows that may terminate on front-end-only routes should resolve relative `returnTo` values against `skillhub.public.base-url`; otherwise back-end direct-hit verification can create a valid session but still strand the browser on a dead server path.
- For local browser verification here, the most reliable setup is `java -jar server/skillhub-app/target/skillhub-app-0.1.0.jar --server.port=<alt>` plus a lightweight static proxy on `3000`; `spring-boot:run` can still trip DevTools/repository classloader issues and Vite's fixed `/api` proxy can hide port-mismatch bugs.
- When browser runtime config points `apiBaseUrl` at an absolute origin, front-end `fetch` and generated API clients must opt into `credentials: 'include'`; otherwise session-establishing redirects can succeed while follow-up `/api/v1/auth/me` calls silently lose the login cookie.

## 2026-04-29 23:29 - US-001
- 实现了 `UassClientFacade`、内部登录上下文与用户画像 DTO，并通过内部 gateway 抽象把未来私有 UASS jar 接入点限制在 auth 模块内。
- 为登录 URL 构建、登录校验、状态检查、用户信息查询和登出补齐成功/失败单测，并确保 facade 异常信息不泄露敏感 callback/state 值。
- 更改的文件
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/UassClientFacade.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/UassClientException.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/UassLoginContext.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/UassUserProfile.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/uass/UassClientFacadeTest.java`
- `scripts/ralph/prd.json`
- `scripts/ralph/progress.txt`
- **未来迭代的学习：**
- 发现的 patterns
- UASS 这一类企业登录接入适合先把“平台 DTO + facade + 单测”打稳，再在后续 story 里接编排服务、state store 和 controller，避免 jar 细节扩散到 app 层。
- 遇到的陷阱
- 仓库当前缺少 `server/.mvn/wrapper`，`make test-backend-app` 会直接失败；本轮改用系统 Maven 执行 `mvn -f server/pom.xml -pl skillhub-auth -am test` 完成同等校验。
- 有用的上下文
- `skillhub-auth` 现有模式倾向于用小 record 表达外部身份数据，例如 `OAuthClaims`；UASS facade 延续这个结构更容易复用后续用户映射与 session 编排。
---
## 2026-04-30 02:08 - US-004
- 按 validator 第二次失败原因最小修复了跨源验证场景下的会话恢复：前端 API client 在 runtime-config 把 `apiBaseUrl` 指向绝对后端地址时，统一为 `fetch` 和 `openapi-fetch` 请求补上 `credentials: 'include'`，避免 UASS callback 建立的 Session 在回到前端 host 后被 `/api/v1/auth/me` 丢失。
- 为绝对 `apiBaseUrl` 场景补了 `web/src/api/client.test.ts` 单测，覆盖普通 `fetchText` 和认证方法发现请求都会携带浏览器凭据。
- 运行了 `make test-frontend`、`make typecheck-web`、`make lint-web`、`mvn -f server/pom.xml -pl skillhub-app -am -Dtest=UassAuthControllerTest,AuthMethodCatalogTest -Dsurefire.failIfNoSpecifiedTests=false test`，全部通过。
- 使用 `agent-browser` 在受控本地环境验证了 `http://localhost:3001/login?returnTo=%2Fdashboard -> Continue with Enterprise SSO -> mock callback -> /dashboard`；页面稳定停留在 dashboard，显示已登录的 `UASS Mock User`，刷新后仍保持登录态。
- 更改的文件
- `web/src/api/client.ts`
- `web/src/api/client.test.ts`
- `scripts/ralph/prd.json`
- `scripts/ralph/progress.txt`
- **未来迭代的学习：**
  - 发现的 patterns（例如，"这个 codebase 使用 X 来做 Y"）
  - 如果浏览器验证需要把前端 host 与后端 host 分开，除了 cookie/SameSite 之外，还要先核对前端 API 层是否显式开启了 credentialed requests；只修 callback 跳转目标不足以保证最终留在已登录页面。
  - 遇到的陷阱（例如，"更改 W 时不要忘记更新 Z"）
  - `openapi-fetch` 默认不会继承你手写 `fetchJson` 里的凭据策略；只修普通 `fetch` 路径会让 `/api/v1/auth/methods` 这类 generated client 请求和后续 session 行为继续不一致。
  - 有用的上下文（例如，"评估面板在 component X 中"）
  - 这次最稳定的本地复现实验是 `39000` 后端 fat jar + `3001` 静态前端代理；它能复现 validator 提到的跨 host 登录链路，同时不会被 Vite 固定代理规则掩盖 cookie 问题。
---
## 2026-04-29 23:42 - US-002
- 实现了 `UassProperties`、`UassLoginStateStore` 抽象、Redis/本地双实现，以及按 `cacheMode` 装配的 `UassStateStoreConfiguration`，把 `uass:state:{state}` TTL 存储能力限定在 auth 模块内。
- 为配置绑定、装配分支、Redis key/TTL、本地过期与清理补齐单测，并在 `application.yml` / `application-local-h2.yml` 中补上 UASS 默认配置与单机本地模式覆盖。
- 更改的文件
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/UassProperties.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/config/UassStateStoreConfiguration.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/store/UassLoginState.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/store/UassLoginStateStore.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/store/RedisUassLoginStateStore.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/store/LocalUassLoginStateStore.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/uass/UassPropertiesBindingTest.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/uass/config/UassStateStoreConfigurationTest.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/uass/store/RedisUassLoginStateStoreTest.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/uass/store/LocalUassLoginStateStoreTest.java`
- `server/skillhub-app/src/main/resources/application.yml`
- `server/skillhub-app/src/main/resources/application-local-h2.yml`
- `scripts/ralph/prd.json`
- `scripts/ralph/progress.txt`
- **未来迭代的学习：**
- 发现的 patterns
- `skillhub-auth` 这类可选中间件依赖适合用“properties + store interface + configuration selector”的三层结构，后续回调编排服务只依赖 `UassLoginStateStore` 即可。
- 遇到的陷阱
- `ApplicationContextRunner` 下如果要测试 `@ConfigurationProperties` 绑定，不能直接把属性类当普通配置塞进去；需要配合 `@EnableConfigurationProperties(...)` 模拟真实启动装配。
- 有用的上下文
- `application-local-h2.yml` 已经关闭 Redis 自动配置，因此把 `skillhub.auth.uass.cache-mode` 设为 `local` 可以让单机联调路径更显式，也避免后续 story 误判运行模式来源。
---
## 2026-04-29 23:52 - US-002
- 实现了 `UassLoginStateService`，把 UASS 登录前 state/returnTo 写入、callback 成功消费、callback 失败清理提升到 store 之外的生产服务层，并复用了现有 OAuth `returnTo` 安全规则。
- 补齐了 `UassLoginStateService` 单测，并安装前端依赖后重新跑通 `make typecheck-web` / `make lint-web`，修复上次验证里 `tsc: not found` 的环境问题。
- 更改的文件
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/UassLoginStateService.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/uass/UassLoginStateServiceTest.java`
- `scripts/ralph/prd.json`
- `scripts/ralph/progress.txt`
- **未来迭代的学习：**
- 发现的 patterns
- UASS 这类浏览器登录新入口可以直接复用 `OAuthLoginRedirectSupport` 的 `returnTo` 过滤和默认跳转语义，避免不同 provider 对开放重定向边界处理不一致。
- 遇到的陷阱
- `make typecheck-web` 不会自动安装依赖；新环境里要先跑 `make web-deps`，否则会因为缺少 `tsc` 等前端二进制而误判 story 失败。
- 有用的上下文
- 现在 `UassLoginStateStore` 已经有明确的上层接入点 `UassLoginStateService`，后续 `UassAuthService`/controller 实现 callback 时应直接接这个服务，而不是自己拼 `UassLoginState` 或直接操作 store。
---
## 2026-04-30 00:02 - US-003
- 实现了 `UassIdentityService`，把 UASS 的 `user_code` 映射为 `uass` provider 下的稳定 subject，并复用 `IdentityBindingService` 完成本地用户查找、首次自动建档和角色快照生成。
- 补齐了已有用户、首次自动创建、禁用用户、待审批用户、缺失 `user_code`、建档失败六个分支测试，确认不会回退到 display name 或 email 匹配。
- 运行了 `mvn -f server/pom.xml -pl skillhub-auth -am -Dtest=UassIdentityServiceTest -Dsurefire.failIfNoSpecifiedTests=false test`、`mvn -f server/pom.xml -pl skillhub-auth -am test`、`make typecheck-web`、`make lint-web`，全部通过。
- 本轮无 UI 变更，未使用浏览器工具；浏览器闭环留给后续 callback/login 页面相关 story 验证。
- 更改的文件
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/UassIdentityService.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/uass/UassIdentityServiceTest.java`
- `scripts/ralph/prd.json`
- `scripts/ralph/progress.txt`
- **未来迭代的学习：**
  - 发现的 patterns（例如，"这个 codebase 使用 X 来做 Y"）
  - 当新的企业登录源已经有稳定主键时，最小改法是先在 provider-specific service 里映射成 `OAuthClaims` 风格数据，再把本地建档委托给现有 `IdentityBindingService`。
  - 遇到的陷阱（例如，"更改 W 时不要忘记更新 Z"）
  - `IdentityBindingService` 会把 `providerLogin` 同时用于用户 display name 和初始 binding loginName；如果后续 UASS 需要区分“展示名”和“上游登录名”，要在复用前先扩展这层模型。
  - 有用的上下文（例如，"评估面板在 component X 中"）
  - `UassLoginContext` 和 `UassUserProfile` 现在都能提供 `user_code`，后续 callback 编排建议优先吃 `UassIdentityService.resolvePrincipal(...)`，避免 controller 自己决定取哪个字段。
---
## 2026-04-30 00:20 - US-003
- 实现了 `UassCallbackFlowService` 和 `UassAuthController`，把 UASS callback 的 state 消费、上游登录校验、用户画像加载、本地用户解析/自动建档和 `PlatformSessionService` 建 session 串成实际生产链路。
- 补齐 callback 链路测试，验证已有用户、首次自动建档、禁用用户、待审批用户、缺失 `user_code`、建档失败都按预期处理，且失败时不会留下部分 Session；同时补了匿名 callback 路由策略测试与 controller 重定向测试。
- 运行了 `mvn -f server/pom.xml -pl skillhub-auth -am -Dtest=UassCallbackFlowServiceTest,RouteSecurityPolicyRegistryTest,UassLoginStateServiceTest -Dsurefire.failIfNoSpecifiedTests=false test`、`mvn -f server/pom.xml -pl skillhub-app -am -Dtest=UassAuthControllerTest -Dsurefire.failIfNoSpecifiedTests=false test`、`mvn -f server/pom.xml -pl skillhub-app -am test`、`make typecheck-web`、`make lint-web`，全部通过。
- 本轮无 UI 变更，未使用浏览器工具；浏览器闭环留给后续 UASS 登录入口 story 验证。
- 更改的文件
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/UassCallbackFlowService.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/UassLoginStateService.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/policy/RouteSecurityPolicyRegistry.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/uass/UassCallbackFlowServiceTest.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/policy/RouteSecurityPolicyRegistryTest.java`
- `server/skillhub-app/src/main/java/com/iflytek/skillhub/controller/UassAuthController.java`
- `server/skillhub-app/src/test/java/com/iflytek/skillhub/controller/UassAuthControllerTest.java`
- `scripts/ralph/prd.json`
- `scripts/ralph/progress.txt`
- **未来迭代的学习：**
- 发现的 patterns
- UASS 这类浏览器回调链路适合把“state 消费 + facade 校验 + identity 解析 + session 建立”收口到单个 flow service，再让 controller 只负责 query 参数映射和 302 响应。
- 遇到的陷阱
- `UassLoginStateService` 这类带测试辅助构造器的 Spring bean，如果没有显式标注注入构造器，默认 profile 下可能一直没暴露，直到启用真实 feature bean 才在运行时炸出 `No default constructor found`。
- 有用的上下文
- 当前 callback endpoint 只在 `skillhub.auth.uass.enabled=true` 且上下文里存在 `UassClientFacade` bean 时装配；后续接入真实 UASS gateway 时，需要保留这个条件式装配边界，避免未接完 provider 时把半成品 endpoint 暴露出去。
---
## 2026-04-30 00:50 - US-004
- 实现了 `UassLoginInitiationService`、`/api/v1/auth/uass/login-url` 和 `/api/v1/auth/uass/redirect`，让 UASS 浏览器登录能在 callback 前生成 state、保存 `returnTo` 并发起上游跳转；同时把匿名路由放行扩展到新的登录入口。
- 补了 `UassGatewayConfiguration` 和 `MockUassGateway`，使 `skillhub.auth.uass.enabled=true` 的真实应用上下文能装配 `UassClientFacade`，并支持用 `skillhub.auth.uass.base-url=mock://self` 做本地 OpenAPI 与浏览器闭环验证。
- 重新生成了 `web/src/api/generated/schema.d.ts`，确认 OpenAPI 中已包含 `/api/v1/auth/uass/login-url`、`/redirect`、`/callback`；并用 `agent-browser` 验证 direct redirect 最终落到 `/dashboard` 且浏览器获得新的 `JSESSIONID`。
- 运行了 `mvn -f server/pom.xml -pl skillhub-auth -am -Dtest=MockUassGatewayTest,UassLoginInitiationServiceTest,RouteSecurityPolicyRegistryTest -Dsurefire.failIfNoSpecifiedTests=false test`、`mvn -f server/pom.xml -pl skillhub-app -am -Dtest=UassAuthControllerTest -Dsurefire.failIfNoSpecifiedTests=false test`、`mvn -f server/pom.xml -pl skillhub-app -am -DskipTests install`、`make typecheck-web`、`make lint-web`。`make test-backend-app` 仍因缺失 `server/.mvn/wrapper/maven-wrapper.properties` 失败，`make generate-api` 仍会误打到外部占用的 `localhost:8080`，因此本轮改用运行在 `39000` 端口的本地 fat jar 生成 schema。
- 更改的文件
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/UassLoginInitiationService.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/UassGatewayConfiguration.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/MockUassGateway.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/policy/RouteSecurityPolicyRegistry.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/uass/UassLoginInitiationServiceTest.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/uass/MockUassGatewayTest.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/policy/RouteSecurityPolicyRegistryTest.java`
- `server/skillhub-app/src/main/java/com/iflytek/skillhub/controller/UassAuthController.java`
- `server/skillhub-app/src/main/java/com/iflytek/skillhub/dto/UassLoginUrlResponse.java`
- `server/skillhub-app/src/test/java/com/iflytek/skillhub/controller/UassAuthControllerTest.java`
- `web/src/api/generated/schema.d.ts`
- `scripts/ralph/prd.json`
- `scripts/ralph/progress.txt`
- **未来迭代的学习：**
  - 发现的 patterns（例如，"这个 codebase 使用 X 来做 Y"）
  - 对这类条件装配的认证入口，单元测试不足以证明路由真的存在；至少要在真实 Spring Boot 进程里验证一次 OpenAPI 或实际 HTTP 路径。
  - 遇到的陷阱（例如，"更改 W 时不要忘记更新 Z"）
  - `make generate-api` 固定打 `localhost:8080`，而当前工作机上这个端口可能被别的项目占用；如果遇到这种环境冲突，需要先在替代端口跑 fat jar，再用 `pnpm exec openapi-typescript http://localhost:<port>/v3/api-docs` 生成。
- 有用的上下文（例如，"评估面板在 component X 中"）
- `local-h2` profile 下直接跑 fat jar 比 `spring-boot:run` 更稳定；后者会带上 DevTools restart classloader，可能把本仓库里某些 repository 代理问题提前放大。
---
## 2026-04-30 01:28 - US-004
- 按 validator 的失败原因最小修复了 UASS 浏览器入口与最终跳转：后端把 `uass-enterprise` 纳入 `/api/v1/auth/methods`，登录页在原有 Password/OAuth 结构外显式渲染企业登录按钮；callback 成功后会把相对 `returnTo` 解析到 `skillhub.public.base-url`，避免浏览器落回后端自身不可访问的 `/dashboard`。
- 补齐 `AuthMethodCatalog`、`UassCallbackFlowService` 和 `login.tsx` 的测试，并为 `local` / `local-h2` 默认配置补上 `skillhub.public.base-url=http://localhost:3000`，让本地验证环境与生产语义一致。
- 运行了 `mvn -f server/pom.xml -pl skillhub-app -am -Dtest=UassCallbackFlowServiceTest,UassLoginInitiationServiceTest,UassAuthControllerTest,AuthMethodCatalogTest,AuthControllerTest -Dsurefire.failIfNoSpecifiedTests=false test`、`make test-frontend`、`make typecheck-web`、`make lint-web`，全部通过。
- 使用 `agent-browser` 完成浏览器闭环验证：`http://localhost:3000/login` 现在可见 `Continue with Enterprise SSO`；点击后经过 `/api/v1/auth/uass/redirect` 和 mock callback 最终落到 `http://localhost:3000/dashboard`，页面显示已登录的 `UASS Mock User`；直接访问 `http://localhost:39000/api/v1/auth/uass/redirect?returnTo=%2Fdashboard` 也能回到前端 dashboard。
- 更改的文件
- `server/skillhub-app/src/main/java/com/iflytek/skillhub/service/AuthMethodCatalog.java`
- `server/skillhub-app/src/main/resources/application-local.yml`
- `server/skillhub-app/src/main/resources/application-local-h2.yml`
- `server/skillhub-app/src/test/java/com/iflytek/skillhub/service/AuthMethodCatalogTest.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/UassCallbackFlowService.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/uass/UassCallbackFlowServiceTest.java`
- `web/src/pages/login.tsx`
- `web/src/pages/login.test.tsx`
- `web/src/i18n/locales/en.json`
- `web/src/i18n/locales/zh.json`
- `scripts/ralph/prd.json`
- `scripts/ralph/progress.txt`
- **未来迭代的学习：**
  - 发现的 patterns（例如，"这个 codebase 使用 X 来做 Y"）
  - 登录页这类动态认证入口优先从 `/api/v1/auth/methods` 衍生 UI，而不是在前端单独硬编码 feature flag；这样 controller、OpenAPI 和页面展示会天然保持一致。
  - 遇到的陷阱（例如，"更改 W 时不要忘记更新 Z"）
  - 浏览器工具默认同源访问 `/api`，如果本地前端不是经 Nginx/Vite 代理启动，runtime-config 里把 `apiBaseUrl` 改成绝对地址后还要注意 cookie/SameSite 行为，否则 callback 成功不一定代表同源页面能读到已登录态。
  - 有用的上下文（例如，"评估面板在 component X 中"）
- 这次 validator 失败点横跨 `AuthMethodCatalog -> /api/v1/auth/methods -> web/src/pages/login.tsx -> UassCallbackFlowService`，后续处理 UASS 状态/登出 story 时，这条链路仍然是最值得优先回归的闭环。
---
## 2026-04-30 02:45 - US-004
- 按 validator 第三次失败的真实浏览器场景完成最小修复：为 `/api/**` 启用了基于 `skillhub.public.base-url` 的集中式 CORS 放行，并把登录页/ OAuth 按钮消费到的相对认证 `actionUrl` 统一改为走 runtime `apiBaseUrl` 归一化，避免独立前端 origin 把 `/api/v1/auth/uass/redirect` 误打到静态前端站点自身。
- 为跨源 `GET /api/v1/auth/methods` 补了后端 MockMvc 断言，为相对认证跳转 URL 补了前端单测；重新验证后，`http://127.0.0.1:3001/login?returnTo=%2Fdashboard` 能显示 `Continue with Enterprise SSO`，点击后经 `http://localhost:39000/api/v1/auth/uass/redirect` 和 callback 回到 `http://localhost:3001/dashboard`，页面显示 `UASS Mock User`，刷新后仍保持登录态。
- 运行了 `mvn -f server/pom.xml -pl skillhub-app -am -Dtest=AuthControllerTest,UassAuthControllerTest -Dsurefire.failIfNoSpecifiedTests=false test`、`mvn -f server/pom.xml -pl skillhub-app -am -DskipTests install`、`make test-frontend`、`make typecheck-web`、`make lint-web`，全部通过。
- 更改的文件
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/config/SecurityConfig.java`
- `server/skillhub-app/src/test/java/com/iflytek/skillhub/controller/AuthControllerTest.java`
- `web/src/api/client.ts`
- `web/src/api/client.test.ts`
- `web/src/features/auth/login-button.tsx`
- `web/src/pages/login.tsx`
- `scripts/ralph/prd.json`
- `scripts/ralph/progress.txt`
- **未来迭代的学习：**
  - 发现的 patterns（例如，"这个 codebase 使用 X 来做 Y"）
  - 对独立前端 origin 的浏览器验收，不要只检查后端 `/api/v1/auth/methods` 是否返回了 enterprise method；还要核对浏览器是否真的被允许跨源读取该响应，以及按钮点击后的相对 `actionUrl` 是否会被重写到后端 origin。
  - 遇到的陷阱（例如，"更改 W 时不要忘记更新 Z"）
  - 本地用静态 `dist` 做浏览器验证时，重新 `build-frontend` 会把 `runtime-config.js` 恢复成仓库默认值；如果要复现跨源场景，记得在验证服务层覆盖运行时配置，否则浏览器会悄悄退回同源 `/api` 路径。
  - 有用的上下文（例如，"评估面板在 component X 中"）
- 这次真正的问题链路是 `SecurityConfig CORS -> authApi.getMethods -> login.tsx enterprise button -> buildAuthRedirectUrl`；`login.tsx` 本身的渲染条件并没有错，问题出在真实浏览器跨源读响应和点击跳转时的 origin 处理。
---
## 2026-04-30 03:50 - US-005
- 实现了 `GET /api/v1/auth/uass/status` 和 `POST /api/v1/auth/uass/logout`，让 UASS 登录态默认以本地 SkillHub Session 为准，并在有可用上游上下文时可选补充远端状态检查。
- 在 callback 成功后把可序列化的 UASS session snapshot 写入 servlet session，登出时先尝试远端 UASS logout，再无条件清理本地 session、security context 和遗留 state key。
- 同步更新了 UASS 路由鉴权、OpenAPI schema，并保留了现有跨源 cookie 配置修复以支撑真实浏览器登出回归。
- 运行了 `mvn -f server/pom.xml -pl skillhub-app -am -Dtest=UassSessionContextServiceTest,UassSessionFlowServiceTest,UassCallbackFlowServiceTest,RouteSecurityPolicyRegistryTest,UassAuthControllerTest -Dsurefire.failIfNoSpecifiedTests=false test`、`make test-frontend`、`make typecheck-web`、`make lint-web`、`mvn -f server/pom.xml -pl skillhub-app -am -DskipTests package`，全部通过。
- 使用浏览器自动化验证了 `http://127.0.0.1:3001/login?returnTo=%2Fdashboard -> Continue with Enterprise SSO -> /dashboard -> POST /api/v1/auth/uass/logout -> /dashboard` 会回到 `http://127.0.0.1:3001/login?returnTo=%2Fdashboard`；`/api/v1/auth/uass/logout` 返回 `204`，受保护路由重新要求登录。
- 更改的文件
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/UassSessionSnapshot.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/UassSessionContextService.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/UassSessionFlowService.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/uass/UassCallbackFlowService.java`
- `server/skillhub-auth/src/main/java/com/iflytek/skillhub/auth/policy/RouteSecurityPolicyRegistry.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/uass/UassSessionContextServiceTest.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/uass/UassSessionFlowServiceTest.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/uass/UassCallbackFlowServiceTest.java`
- `server/skillhub-auth/src/test/java/com/iflytek/skillhub/auth/policy/RouteSecurityPolicyRegistryTest.java`
- `server/skillhub-app/src/main/java/com/iflytek/skillhub/controller/UassAuthController.java`
- `server/skillhub-app/src/main/java/com/iflytek/skillhub/dto/UassLoginStatusResponse.java`
- `server/skillhub-app/src/test/java/com/iflytek/skillhub/controller/UassAuthControllerTest.java`
- `server/skillhub-app/src/main/java/com/iflytek/skillhub/config/SessionCookieConfiguration.java`
- `server/skillhub-app/src/test/java/com/iflytek/skillhub/config/SessionCookieConfigurationTest.java`
- `server/skillhub-app/src/main/resources/application-local.yml`
- `server/skillhub-app/src/main/resources/application-local-h2.yml`
- `server/skillhub-app/src/test/resources/application-test.yml`
- `web/src/api/generated/schema.d.ts`
- `scripts/ralph/prd.json`
- `scripts/ralph/progress.txt`
- **未来迭代的学习：**
  - 发现的 patterns（例如，"这个 codebase 使用 X 来做 Y"）
  - 如果企业登录后续还要支持 remote introspection / single logout，最稳妥的是把 provider-specific session snapshot 作为独立 session attribute 保存，再由 flow service 读取；`PlatformPrincipal` 继续只承载本地鉴权快照。
- 遇到的陷阱（例如，"更改 W 时不要忘记更新 Z"）
  - 这个环境里的 `agent-browser` 需要 `--no-sandbox` 才能启动，但即使加上后 CDP 导航也可能超时；浏览器验收要准备好回退到仓库自带的 Playwright + 系统 Chrome 路径。
  - 有用的上下文（例如，"评估面板在 component X 中"）
  - 本地静态前端 `127.0.0.1:3001` 当前读取的是 `web/dist/runtime-config.js`，而不是仓库根下的 `.validator/web-runtime-config.js`；需要切换后端端口时，别只改 validator 文件。
---
## 2026-04-30 04:16 - US-005
- 按 validator 失败原因最小修复了前端登出入口：`authApi.logout` 现在会根据 provider 选择 `/api/v1/auth/uass/logout` 或旧 `/api/v1/auth/logout`，并统一通过 `buildApiUrl` 命中 runtime `apiBaseUrl` 下的真实后端。
- 调整用户菜单登出后的浏览器行为为“清缓存后回访当前受保护路径”，让现有路由守卫稳定把浏览器送回 `/login?returnTo=...`，避免再硬跳 `/` 掩盖未清理的登录态。
- 补了前端单测覆盖 UASS/非 UASS 两条 logout 路径，并在 `3001 -> 39000` 的跨源本地环境用浏览器验证：点击 `Logout` 实际请求 `POST http://localhost:39000/api/v1/auth/uass/logout` 返回 `204`，随后页面回到 `http://127.0.0.1:3001/login?returnTo=%2Fdashboard`；同一浏览器会话读取 `http://localhost:39000/api/v1/auth/uass/status` 返回 `authenticated=false`。
- 运行了 `make test-frontend`、`make typecheck-web`、`make lint-web`、`make build-frontend`，全部通过。
- 更改的文件
- `web/src/api/client.ts`
- `web/src/api/client.test.ts`
- `web/src/shared/components/user-menu.tsx`
- `scripts/ralph/prd.json`
- `scripts/ralph/progress.txt`
- **未来迭代的学习：**
  - 发现的 patterns（例如，"这个 codebase 使用 X 来做 Y"）
  - 对独立前端 origin 的认证回归，不能只验证登录 redirect；像 logout 这类 POST 编排也要经过同一套 `buildApiUrl + credentials` 路径，才能真正覆盖真实浏览器行为。
  - 遇到的陷阱（例如，"更改 W 时不要忘记更新 Z"）
  - `make build-frontend` 会把 `web/dist/runtime-config.js` 重置成默认空配置；如果要继续用静态 `dist` 做跨源浏览器验证，需要在验证前恢复目标 `apiBaseUrl/appBaseUrl`。
  - 有用的上下文（例如，"评估面板在 component X 中"）
  - 这次用户菜单登出链路的关键点在 `web/src/shared/components/user-menu.tsx -> authApi.logout -> buildApiUrl`，并不是 UASS controller/service 本身；后续再遇到“后端 endpoint 已有但浏览器没命中”的问题，应先从前端 runtime URL 归一化排查。
---
