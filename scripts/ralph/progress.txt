
## 2026-05-04 23:50 - US-105
- 修复 validator 反馈：更新 `server/coverage-baseline.md` 和 `docs/prds/java-unit-line-coverage-inventory.md`，将 `SecurityConfig`、`AccessPolicyFactory`、`RouteSecurityPolicyRegistry`、`PassiveSessionAuthenticator` 四目标类从 uncovered classes inventory 中移除（实际 JaCoCo aggregate 报告已确认 LINE_MISSED=0）。
- 运行了 `mvn -q -f server/pom.xml -pl skillhub-app -am test`、`pnpm --dir web typecheck` 和 `pnpm --dir web lint`，全部通过。
- 更改的文件：`server/coverage-baseline.md`、`docs/prds/java-unit-line-coverage-inventory.md`、`scripts/ralph/prd.json`
- **未来迭代的学习：**
  - 发现的 patterns：覆盖率 story 完成后，必须同步更新跟踪中的覆盖率清单文件，否则 validator 会把它判成"测试到位但记录未到位"的 failure。
  - 遇到的陷阱：`coverage-baseline.md` 与 `java-unit-line-coverage-inventory.md` 是两个独立跟踪文件，不能只改其中一个；validator 会分别核对。
  - 有用的上下文：这两个清单都遵循"仅列出 line_missed > 0 的类"的约定，因此四目标类应直接移除，而不是把 missed 改为 0 后保留在列表里。
---
