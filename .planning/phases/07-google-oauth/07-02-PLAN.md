---
phase: 07-google-oauth
plan: "02"
type: execute
wave: 1
depends_on:
  - "01"
files_modified:
  - web/src/features/auth/login-button.tsx
  - web/src/features/auth/login-button.test.ts
  - web/src/pages/login.tsx
  - web/src/pages/login.test.tsx
  - web/src/i18n/locales/en.json
  - web/src/i18n/locales/zh.json
autonomous: true
requirements:
  - OAUTH-04
  - OAUTH-05
  - OAUTH-06
  - QA-02
must_haves:
  truths:
    - "OAuth login UI stays backend-driven: rendered providers come from /api/v1/auth/methods only."
    - "Google provider UX is explicit and test-backed without breaking existing provider entries."
    - "Login flow still redirects through provider actionUrl and preserves returnTo behavior."
  artifacts:
    - path: web/src/features/auth/login-button.tsx
      provides: "Provider-aware OAuth button rendering."
      contains: "methodType === 'OAUTH_REDIRECT'"
    - path: web/src/features/auth/login-button.test.ts
      provides: "UI contract tests for provider list and redirect behavior."
      contains: "oauth-google"
  key_links:
    - from: web/src/pages/login.tsx
      to: web/src/features/auth/login-button.tsx
      via: "OAuth tab renders backend-provided methods with returnTo."
---

# Phase 7 Plan 02: Web Login UX For Google OAuth

<objective>
Интегрировать Google OAuth в web login experience так, чтобы provider rendering оставался data-driven, а поведение redirect было предсказуемо и протестировано.
</objective>

<context>
@.planning/phases/07-google-oauth/07-CONTEXT.md
@.planning/phases/07-google-oauth/07-RESEARCH.md
@web/src/pages/login.tsx
@web/src/features/auth/login-button.tsx
@web/src/features/auth/use-auth-methods.ts
@web/src/i18n/locales/en.json
@web/src/i18n/locales/zh.json
</context>

<threat_model>
<asvs_level>1</asvs_level>
<threats>
  <threat id="T-07-02-01" severity="high">UI hardcodes provider routes and diverges from backend action URLs.</threat>
  <threat id="T-07-02-02" severity="medium">Google entry rendering breaks existing providers or fallback behavior.</threat>
  <threat id="T-07-02-03" severity="medium">Localization gaps produce mixed-language login CTA text.</threat>
</threats>
<mitigations>
  <item>Сохранять единственный source of truth: `provider.actionUrl` from backend.</item>
  <item>Расширить provider-aware tests на multiple providers включая `oauth-google`.</item>
  <item>Обновить i18n keys и проверить оба локаля (en/zh) на login OAuth copy.</item>
</mitigations>
<block_on>high</block_on>
</threat_model>

<tasks>
<task type="test">
  <name>Task 1: Добавить failing tests для Google entry в login UI</name>
  <files>
    web/src/features/auth/login-button.test.ts
    web/src/pages/login.test.tsx
  </files>
  <read_first>
    @web/src/features/auth/login-button.test.ts
    @web/src/pages/login.test.tsx
    @web/src/features/auth/login-button.tsx
  </read_first>
  <action>
    Расширить `login-button.test.ts` сценариями:
    1) methods list содержит `oauth-google` и `oauth-github` -> рендерятся две отдельные кнопки.
    2) click по Google button устанавливает `window.location.href` ровно в `provider.actionUrl` (`/oauth2/authorization/google?...`).
    3) non-OAUTH methods (например PASSWORD) не рендерятся в этом компоненте.
    В `login.test.tsx` добавить проверку, что вкладка OAuth отображает Google action после загрузки auth methods.
  </action>
  <acceptance_criteria>
    - `login-button.test.ts` содержит test case с `oauth-google`.
    - Тесты проверяют redirect в `/oauth2/authorization/google`.
    - `login.test.tsx` содержит assertion на Google provider label/action.
  </acceptance_criteria>
  <verify>
    <automated><![CDATA[cd web && pnpm run test -- src/features/auth/login-button.test.ts src/pages/login.test.tsx]]></automated>
  </verify>
  <done>Google login entry поведение зафиксировано failing tests.</done>
</task>

<task type="code">
  <name>Task 2: Реализовать provider-aware rendering в login-button</name>
  <files>
    web/src/features/auth/login-button.tsx
  </files>
  <read_first>
    @web/src/features/auth/login-button.tsx
    @web/src/api/types.ts
    @web/src/features/auth/use-auth-methods.ts
  </read_first>
  <action>
    Обновить `login-button.tsx`, чтобы provider rendering явно учитывал provider id:
    - Ввести helper `getProviderVisual(provider.id)` с минимум двумя ветками: `oauth-google` и default.
    - Для `oauth-google` использовать отдельный icon/label styling без hardcoded route logic.
    - Сохранить redirect handler только через `provider.actionUrl`.
    - Добавить `data-provider-id={provider.id}` на кнопку для надежных test selectors.
  </action>
  <acceptance_criteria>
    - `login-button.tsx` содержит `data-provider-id`.
    - Компонент имеет ветку для `oauth-google`.
    - Redirect logic продолжает использовать `provider.actionUrl` без строковых `'/oauth2/authorization/google'` констант в onClick.
  </acceptance_criteria>
  <verify>
    <automated><![CDATA[cd web && pnpm run test -- src/features/auth/login-button.test.ts]]></automated>
  </verify>
  <done>Login button корректно рендерит Google provider наравне с остальными OAuth methods.</done>
</task>

<task type="code">
  <name>Task 3: Завершить login UX copy и интеграционные проверки</name>
  <files>
    web/src/pages/login.tsx
    web/src/i18n/locales/en.json
    web/src/i18n/locales/zh.json
    web/src/pages/login.test.tsx
  </files>
  <read_first>
    @web/src/pages/login.tsx
    @web/src/i18n/locales/en.json
    @web/src/i18n/locales/zh.json
  </read_first>
  <action>
    Привести login OAuth copy к provider-agnostic формату:
    - Убедиться, что `loginButton.loginWith` корректно подставляет displayName для Google.
    - Добавить/обновить i18n ключи в `en.json` и `zh.json`, чтобы текст OAuth таба и кнопок не был provider-specific к GitHub.
    - В `login.test.tsx` добавить assertions на отсутствие hardcoded "GitHub only" текста в OAuth tab при mock methods с Google.
  </action>
  <acceptance_criteria>
    - `en.json` и `zh.json` содержат актуальные login button ключи для provider-agnostic CTA.
    - `login.test.tsx` проверяет корректный displayName rendering для Google.
    - В OAuth tab логике отсутствуют hardcoded provider name checks.
  </acceptance_criteria>
  <verify>
    <automated><![CDATA[cd web && pnpm run test -- src/pages/login.test.tsx src/features/auth/login-button.test.ts]]></automated>
    <automated><![CDATA[cd web && pnpm run typecheck]]></automated>
  </verify>
  <done>Google OAuth UX готов в web login и подтвержден тестами.</done>
</task>
</tasks>

<verification>
- `cd web && pnpm run test -- src/features/auth/login-button.test.ts src/pages/login.test.tsx`
- `cd web && pnpm run typecheck`
</verification>

<success_criteria>
- Login page показывает Google provider из backend catalog без hardcoded paths.
- Redirect flow работает через `actionUrl`.
- i18n copy и tests подтверждают отсутствие regression для OAuth tab.
</success_criteria>
